# KeyBase Docker Compose Guide

Please do note that you have to create a `.env` file in the same directory as your `docker-compose.yml` file with the following variables:

```
# .env file
# Replace these values with your own
NEXT_PUBLIC_API_BASE=http://IP:PORT
```
